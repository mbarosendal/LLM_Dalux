"""HTTP API package for the online Dalux integration.

Keep transport-specific code here: FastAPI app, routes, auth, and request/response schemas.
Business logic should live in `mcp_dalux.services` so the API layer stays thin.
"""
